"""Backtest execution plane (Stage 5a, docs 15-17).

RUN admission, immutable Run Manifest, the deterministic V1 engine stub and the
canonical result metric registry. Pure domain logic only — no persistence.
"""
