{
  "_README": [
    "FROZEN acceptance-coverage debt ceiling — enforced by",
    "docs/audit/acceptance_semantic_scan.py --ratchet (CI job 'Backend', ci.yml).",
    "",
    "Semantics: ceilings.status.<status> and ceilings.debt_class.<A|B|C|D> are the",
    "MAXIMUM number of criteria that may carry that status / class. Measured >",
    "ceiling => CI FAILS. ceilings.total_criteria is a FLOOR, not a ceiling: the",
    "corpus may grow, never shrink, so deleting an inconvenient `partial` row",
    "cannot read as progress. Measured < ceiling => CI prints the tightened block",
    "for you to paste back here; lowering a ceiling after real work is expected.",
    "",
    "Every number here is MEASURED DEBT, not an allowance. There is no headroom by",
    "design: a ceiling set above the measured figure would silently license the",
    "next unproven criterion. Raising one is an adjudication recorded in",
    "docs/audit/acceptance_coverage_debt_ledger.md — never a CI fix.",
    "",
    "This ratchet bounds how much of the acceptance contract is UNPROVEN. It is not",
    "a project-completion figure: a criterion counted here may well be implemented",
    "correctly — the claim is only that nothing in the suite asserts it."
  ],
  "provenance": {
    "measured_by": "docs/audit/acceptance_semantic_scan.py --root .. --report",
    "measured_at": "2026-08-19T17:40:00Z",
    "slice": "acceptance batch 15 (doc 17 - Arrange Metrics, BACKEND surface). Four criteria closed: AM-03, AM-05, AM-06 and AM-07, each of which had exactly the shape this ledger calls class B - the behaviour ships, the assertion was missing. No product code changed; the diff is four cases plus two helpers in backend/tests/integration/test_arrange_metrics.py, which reuses that file's existing _seed_registry / _seed_principals / _workspace / _seed_result builders. ALL FOUR ROWS HAD THE SAME SHAPE: the claim had been measured in a world where its opposite could not be produced. AM-05.c2: the pre-existing test_min_selection_blocked empties the selection against the SYSTEM DEFAULT sentinel - a profile with no revision at all - so 'the previous canonical revision survives' was structurally out of its scope. The new test drives the refusal against a profile that already carries TWO revisions and then re-reads the root (head, row_version, max_revision_no, resolved projection). AM-07.c2: the one ownership guard the suite drove was an UNLOCKED profile with a CHANGED selection, which is AM-14's scenario. The new test drives the criterion's own scenario - LOCKED profile, pure unlock, foreign caller - and then shows the head, its row_version and the lock flag did not move; the byte-identical call from the OWNER succeeds afterwards, so the denial is attributable to who called rather than to a malformed request. AM-06.c3: all nine selectable codes are applied first so the projection cannot narrow, then the full metric card list is compared before / while locked / after unlocking, with the cycle itself asserted (reason lock then unlock, profile.is_locked flipping) and the metric_value rows re-read from the database at the end. The NULL metric is inside the compared set. AM-03.c2+c3: the e2e pipeline asserted manifest-hash stability across a span that also contains a Trash delete and restore, so the claim was never scoped to the Apply, and nothing anywhere counted run/job rows around one. The new test seeds a SUCCEEDED BacktestRun and its Job row first, so 'no run is created' is measured against a non-empty table rather than against nothing. FIVE negative controls, each run by hand against an otherwise pristine tree, and each read for WHICH assertion went red. (1) Moving the empty-selection raise out of normalize_selection to below the append inside _op keeps test_min_selection_blocked GREEN - the exception is still raised - and reddens exactly the head assertion, which is why pytest.raises alone could never have closed AM-05.c2. (2) Skipping ensure_can_edit when the head revision is locked (lock as a grant) leaves test_foreign_profile_role_guard green, because that test drives an unlocked profile, and reddens only the new AM-07 test - an old test staying green under an injected defect is EVIDENCE, not a flaw: it never traverses the branch the defect is on. (3) A Lock that rewrites metric values reddens the mid-cycle card comparison and NOTHING else in the file. (4) An Apply that enqueues a Job reddens the job-list assertion. (5) An Apply that rewrites BacktestRun.manifest_hash reddens the manifest tuple. Product code was restored byte-for-byte after each control (git diff over backend/src is empty). NO NEW FINDING in this batch: all four rows were correctly classified B and all four closed as written. Doc 17 keeps three partial and two uncovered rows, none of which is backend test debt. NUMBERING: authored as ADIM 91 / batch 15. The ADIM number moved TWICE while this branch was open - 89 was taken by #799 (which adds docs/ADIM89_LANDED_KICKOFF.md) and 90 by #802 - and was measured from the FILE PATHS the open PRs add, not from their titles, which is the check no CI gate can perform. The batch number did not move: #797 landed as batch 14 and no other open PR is an acceptance batch.",
    "base_commit": "ee5ab38",
    "conditions": "static scan for the ledger figures (ast + title extraction against the live test tree); the four new cases and all five negative controls WERE executed against a real PostgreSQL 16 provisioned inside this session container (backend/tests/integration/test_arrange_metrics.py: 21 passed, was 17). Backend lint / format / type gates were run locally and were clean. A whole-suite run was also started locally; the frontend was untouched (zero frontend lines), so CI remains the authority for the passing count and for the coverage gate. Recorded trap: the FIRST whole-suite attempt reported ~40 failures that had nothing to do with this diff - the ad-hoc database had not been migrated (`alembic upgrade head`), so contract tests hit 'relation \"human_users\" does not exist'; probing an untouched contract file reproduced it, which is how it was attributed to the environment rather than to the change.",
    "supersedes": "the batch 14 freeze (83 partial / 7 uncovered; A1 B51 C6 D32, base a5b46ab), which is the freeze immediately below this one on main. THE CHAINING NOTE FIRED AGAIN, AND THE RE-MEASUREMENT MATTERED: this branch was first frozen against a5b46ab at 79 partial / 8 uncovered / B 48, then #797 (doc 05 frontend, TL-18, ADIM 88 / batch 14) merged while the PR was open. The branch was reset onto ee5ab38, the map patch re-applied there, and --ratchet RE-RUN on the merged tree: 79 partial / 7 uncovered / B 47. Note that the partial figure is coincidentally the same while B and uncovered are not - subtracting the two freezes by hand would have produced a wrong ceiling. The two batches close DISJOINT criteria (#797: TL-18 on doc 05; this one: AM-03/05/06/07 on doc 17), which is why both closures survive intact. Still open against these same serial files at freeze time: nothing - #799/#800/#801 (closure C4) and #802 (a docs ritual) touch neither the map nor this baseline.",
    "clause_totals_at_freeze": {
      "covered": 1039,
      "partial": 6,
      "uncovered": 91,
      "deliberate_future_dev": 27,
      "not_applicable": 12,
      "product_decision_required": 0
    }
  },
  "adjudication": {
    "why_a_ceiling_and_not_a_failure": "Turning 139 open criteria into a hard CI failure blocks every unrelated PR behind work no single slice can do. Leaving them unbounded is how the number grew unnoticed. The ceiling is the same trade frontend/e2e/a11y-baseline.json already makes: today's debt is frozen, and only GROWTH is red.",
    "class_D_is_not_test_debt": "32 of the open criteria name an error code, model field, validator or Agent Tool Gateway member that does not exist in backend/src. No test can close them; counting them as coverage debt budgets product work to a test slice. They are ratcheted separately so that closing class B cannot mask class D growing.",
    "class_C_is_never_closable": "6 criteria rest on a clause that is not assertable by nature — a statement about the V18 mockup document, another page's document scope, a V1 feature deliberately left closed (retention auto-purge, doc 20 §16), or a scenario Production cannot construct (CP-06, SUPPORTED_TARGET_RUNTIMES == {python}). Their ceiling must never be driven to zero; driving it down would mean someone invented a product to satisfy a document sentence.",
    "O-31_active_run_refusal_name": "Three page documents named ONE refusal three ways: ACTIVE_RUN_DEPENDENCY (doc 03 §14, doc 04 §15), DELETE_BLOCKED_BY_RUNNING_JOB (doc 20 §15) and OBJECT_IN_ACTIVE_RUN (doc 01/15). Only the last ships. Settled the O-02 way — the SHIPPED name is canonical, the other two are historical — and pinned by backend/tests/integration/test_backtest_persistence.py::test_active_run_blocks_work_object_delete, which now asserts the wire code and http_status 409 instead of the exception class alone.",
    "class_B_batches_are_deliberately_small": "101 open class-B criteria cannot be closed in one pass without the review degenerating into a rubber stamp — and a criterion marked covered by a test that does not actually prove it is worse than one honestly left partial. Batches are therefore scoped to one page document and one surface (batch 01 = doc 05, backend). Each closed clause cites a node that fails when the behaviour is removed; where the assertion could plausibly pass vacuously it was negative-controlled by hand (see the notes on TL-03, TL-15 and TL-17 in acceptance_semantic_map.yaml).",
    "TL_11_c3_is_very_likely_class_C_not_B": "ADIM 49 finding, recorded rather than acted on. TL-11.c3 names a run over an ALLOCATION-ENABLED composition. Shared capital allocation is fail-closed at ADMISSION in this build (domain/allocation/capability.py SHARED_ALLOCATION_STATUS = 'future_dev'; commands/backtest_run.py raises ALLOCATION_SHARED_MODE_NOT_IN_BUILD before any run, manifest or job exists), so the run the clause describes cannot be constructed and NO test can close it. That is the class C definition, not class B. It was NOT moved: B -> C would RAISE the C ceiling, and ceilings only fall except by adjudication. ADIM 48's kickoff recommended this row as closable; that was wrong. Two such findings are now open — this and TL-16 (c4 wants a 409 field that does not exist, so class D).",
    "three_more_doc12_clauses_look_misclassified": "ADIM 54 findings, recorded rather than acted on — the same shape as the TL-11.c3 finding of ADIM 52. RD-01.c4 wants an ANALYSIS-time DEPENDENCY_BLOCKED gate that does not exist and is not needed (market_entity_id is required at create). RD-05.c5 wants an Agent research draft/analysis tool; ToolName exposes none. RD-11.c2 wants the evidence bundle to pin feature definitions, time policies and mapping ids; _seal_bundle members carry five fields and none of those three. None was moved: B -> C/D RAISES a ceiling, and ceilings only fall except by adjudication. FIVE such findings are now open across the ledger (TL-11.c3, TL-16, TL-01.c4 and these three clauses), which is itself the signal: ADIM 42 classified from each record's own notes without re-reading product code, and said so.",
    "PC_02_c2_empty_input_text_is_unconstructible": "ADIM 75 finding, recorded rather than acted on — the ninth of this shape, and the second on doc 07. PC-02.c2 wants the Pre-Check surface to show a 'final empty-input text' for an EMPTY SOURCE (doc 07 §16: 'UI shows TA PRE-CHECK RESULT with final empty-input text'). Measured on base 0f0651d, two independent reasons it is not class B. (1) The copy does not exist: EMPTY_SOURCE appears NOWHERE in frontend/src, and the only EmptyState on the Pre-Check page (pages/PreCheck.tsx:123) reads 'No requests yet' — an empty request LIST, not an empty source. (2) Decisively, the state is UNCONSTRUCTIBLE on the shipped surface: the overlay renders as `{precheckOpen && detail !== null ? <PreCheckModal … />}` (pages/CreatePackage.tsx:765), so Pre-Check cannot be opened without a persisted request, while a request with an empty source can never be persisted because normalize_request raises EMPTY_SOURCE at the route before the DB. There is no reachable screen on which an empty-input Pre-Check result could render, which is the class C definition ('a scenario Production cannot construct'), not test debt. It was NOT moved: B -> C would RAISE the C ceiling, and ceilings only fall except by adjudication. Note this differs from PC-20.c3 on the same document: that one IS constructible and simply unimplemented (class D), whereas this one cannot be reached at all.",
    "PC_20_c3_restored_request_staleness_is_class_D": "ADIM 72 finding, recorded rather than acted on — the eighth of this shape. PC-20.c3 says 'a restored package REQUEST comes back stale'. Doc 07 §5's 'deleted / restored request' lifecycle row does demand it ('restored request current dependencies için stale kabul edilir'), but NOTHING IMPLEMENTS IT. Measured on base df7df92: a package request IS a trashable registry root (entity_type='package_request'), so the scenario is constructible and this is not class C. Restore, however, goes through commands/deletion.py::_restore_registry_target — the generic branch — which only returns deletion_state to ACTIVE and clears deleted_at/deleted_by/delete_reason. There is no package-request branch (the typed ones are backtest_result, manual_document, hypothesis_artifact). cp_repo.get_current_scan then reads detail.current_scan_id through a plain session.get with NO deletion filter, so the pre-delete PASSED scan survives intact, and _enforce_precheck_gate compares only context_hash and registry_fingerprint — neither of which a delete/restore cycle moves. A restored request therefore passes the Send gate instead of coming back stale. Closing this needs a restore-time staleness marker (product work), so the row is class D, not test debt. It was NOT moved: B -> D would RAISE the D ceiling, and ceilings only fall except by adjudication.",
    "doc16_event_names_are_shipped_names": "ADIM 55. Doc 16 names two audit events RESULT_SOFT_DELETED and RESULT_RESTORED. Neither string exists: the shipped kinds are `backtest.result_soft_deleted` (written by the command) and `trash.restored` (written by the generic trash restore path). Settled the O-02 / O-31 way — the SHIPPED name is canonical and the document's is historical — and the restore assertion additionally pins target_entity_id to the result id, so it proves THIS result's restore was audited rather than that some trash activity occurred. Renaming shipped event kinds to match a document sentence would break every consumer for a spelling.",
    "AL_06_c3_is_frontend_not_backend_debt": "ADIM 83 note, not a reclassification. AL-06 keeps debt_class B with one open clause: c3 ('the textarea content is preserved for the user after the rejection') is a frontend behaviour and batch 11 was scoped to doc 18's BACKEND surface, the same one-document-one-surface rule every batch since batch 01 has followed. The clause is closable — analysisLab.test.tsx simply has no directive-validation case yet — so it is ordinary class-B test debt awaiting a doc 18 FRONTEND batch, not a finding. Doc 18's backend-testable debt is now zero. CLOSED by acceptance batch 13 (this freeze), which took doc 18's FRONTEND surface and added the missing directive-validation case. The note is kept rather than deleted so the record shows the clause was carried deliberately for exactly one batch, not overlooked.",
    "TL_16_c4_409_carries_no_canonical_state": "ADIM 84 finding, recorded rather than acted on -- the tenth of this shape, and the third on doc 05. TL-16.c4 says 'the 409 envelope carries the server's canonical current state'. MEASURED on base 7f331c7: every raise site of WorkObjectRevisionConflictError is bare -- 'raise WorkObjectRevisionConflictError()' in commands/trade_log.py, commands/trading_signal.py and commands/mainboard.py, with no argument at any of the three. shared/errors.py::AppError.__init__ then sets self.details = details or [] and leaves scope_id / field_path / remediation at their class defaults, and WorkObjectRevisionConflictError declares none of them. So the published 409 carries code, message, category, retryable and suggested_action -- and nothing whatsoever about the root's current head revision id. There is no field for a test to read, which is the class D definition (the code the criterion names does not exist), not test debt. Note this differs from TL-11.c3 on the same document: that clause is unconstructible (class C shape), whereas this one is perfectly constructible and simply unimplemented. It was NOT moved: B -> D would RAISE the D ceiling, and ceilings only fall except by adjudication. TL-16 therefore stays partial / class B even though c3 closed in this slice. Fixing it would widen the O-02 error envelope, so it is a product decision rather than a test slice's call.",
    "TL_22_c4_is_defended_by_two_gates_not_one": "ADIM 84 measurement, recorded because it changes how the row must be negative-controlled. 'The human Mainboard is not auto-mutated by the Agent's work' is enforced twice over: commands/trade_log.py::_resolve_attach_workspace resolves the default board THROUGH THE ACTOR (queries/mainboard.py::get_default_mainboard filters on actor.principal_id), and commands/mainboard.py::_require_owned_workspace independently refuses a workspace the actor does not own. Breaking only the first therefore turns the test red at 'status == succeeded' with a REJECTED tool call -- red for the wrong reason, proving the ownership gate rather than the assertion under test. The controls that count disabled BOTH gates (the workspace-identity assertion went red) and leaked a row_version bump onto a bystander board inside mainboard.py::_recompute_composition_hash (the board-state assertion went red). A future batch re-measuring this row should not read the first control's red as evidence.",
    "TL_18_was_a_bookkeeping_miss_not_missing_coverage": "ADIM 88 measurement. TL-18 sat at uncovered/class B with the note 'Nothing in the suite asserts this', but frontend/src/test/presentationState.test.tsx had asserted exactly it since ADIM 60 (d012a63, PR #719) -- its describe block names TL-18 by id, and its two sibling criteria TS-16 and AOS-16 cite those very nodes as covered. The note was simply never revisited after the test landed. Recorded because the failure mode is systemic, not local: a criterion can be proven by a node whose own describe block names it and still read as debt for five waves. Before planning any batch, grep the test tree for the criterion id -- it is cheaper than writing a test that already exists. Falsifiability was measured rather than assumed: adding a PATCH to the three onClick handlers in pages/Mainboard.tsx turns both nodes red, so this is a one-point break, not the three-point (unfalsifiable) shape recorded for TL-02.c2. A first attempt at that control modified NOTHING because the handler string occurs three times and the patch asserted a single occurrence; the run stayed green and would have been a false negative control had the assertion not caught it."
  },
  "ceilings": {
    "total_criteria": 383,
    "status": {
      "partial": 79,
      "uncovered": 7
    },
    "debt_class": {
      "A": 1,
      "B": 47,
      "C": 6,
      "D": 32
    }
  }
}
