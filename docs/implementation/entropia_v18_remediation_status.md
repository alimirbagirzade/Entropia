# Entropia V18 — Remediation Traceability

Mandated by `docs/spec/Entropia_V18_Claude_Code_Implementation_Spec.md` §3.2. One row per
requirement (F-01..F-25, UI-01..UI-22). **Status** ∈ {Not Started, In Progress, Blocked, Complete}.
A requirement is **Complete** only with working end-to-end behavior + passing acceptance tests
(§3.3) — never because a component/route/field exists.

> Ground truth verified against `origin/main` @ `0daa454` (PR #208) on 2026-07-14, not the
> loaded session handoff (stale-by-default per CLAUDE.md).

## Spec document-level errors found (the "hataları")

These are errors **in the spec document itself**, surfaced during review:

1. **§2 references `.docx` sources that ship as `.md`** — `Entropia_V18_Master_Technical_Reference`
   exists at `docs/spec/Entropia_V18_Master_Technical_Reference_v1_0.md` (not `.docx`). The 22 page
   specs are `.md` (`01_..._v1_1.md` … `22_..._v1_1.md`), present and correct.
2. **§2 lists `2.3. POSITION ENTRY LOGIC.docx` — MISSING** from `docs/spec/`. No equivalent present.
3. **§2 lists `Entropia_V18_..._Handoff_ve_Calisma_Standardi_v1_1.docx` — MISSING**. The only
   `.docx` present is `Coder_AI_Asamali_Kodlama_Prompt_Set_v1_0.docx` (not the one §2 names).
4. **§2 "V18 prototype HTML"** = `docs/spec/index_guncellenmis_duzeltilmis_v18.html` (present; name
   differs from the generic reference).
5. The spec's own §3.2 traceability file (`docs/implementation/...`) **did not exist** until this
   file — created as the first Phase-0 deliverable.

Otherwise the spec's technical "broken" claims are **accurate, not errors** (verified below).

## Status table

| ID | Title | P | Status | Verified finding / evidence |
|----|-------|---|--------|------------------------------|
| **F-01** | **Real Market Data file upload** | **P0** | **Complete (this PR)** | Fixed: `start_market_raw_upload` (`application/commands/market_data.py`) now accepts real bytes via a multipart `POST /market-datasets/{entity_id}/raw-uploads` (`UploadFile`), never a pre-supplied `object_key`/`content_digest`/`size_bytes`; object key + SHA-256 digest + byte size + content type are all derived server-side, written via `datasets.put_raw_bytes`, and integrity-verified with a storage read-back before any evidence row is persisted (`MARKET_DATA_UPLOAD_INTEGRITY_FAILED` on mismatch). Content-addressed dedup (`find_raw_asset_by_hash`, scoped per dataset) makes a retry idempotent regardless of Idempotency-Key. Unsupported type/empty/oversized file -> `MARKET_DATA_FILE_TYPE_NOT_ALLOWED`/`VALIDATION_ERROR`/`MARKET_DATA_FILE_TOO_LARGE`; storage failure -> `MARKET_DATA_UPLOAD_STORAGE_FAILED`. Frontend: `frontend/src/lib/upload.ts` — new shared XHR-based multipart primitive (`uploadFile` + `useFileUpload` hook, real progress/cancel/retry) reused by `MarketData.tsx`'s `UploadComposer`, which replaces the manual object-key/digest/size-bytes form with a native file chooser; no storage metadata is ever requested from the user. No migration (`market_raw_asset.object_key`/`content_digest`/`size_bytes` columns already existed). Tests: +5 backend unit (validators), +7 backend integration (happy path, dedup, unsupported type, non-owner, storage failure, digest mismatch), +2 backend contract (pure-validation-before-DB), +3 frontend unit (`upload.test.ts` — progress/error/cancel), +2 frontend component (success+finalize, error+retry). A Playwright E2E extension of `frontend/e2e/specs/02-market-data-upload.spec.ts` is authored and held on a reference branch pending F-23's merge to `main` (that directory does not exist on `main` yet). |
| **F-02** | **Real Research Data file upload** | **P0** | **Complete (this PR)** | Fixed: `create_upload_session` (`application/commands/research_data.py`) now accepts real bytes via a multipart `POST /research-datasets/{entity_id}/upload-session` (`UploadFile`), never a pre-supplied `object_key`/`content_digest`/`size_bytes`; object key + SHA-256 digest + byte size + content type are all derived server-side, written via `datasets.put_raw_bytes`, and integrity-verified with a storage read-back before any evidence row is persisted (`RESEARCH_DATA_UPLOAD_INTEGRITY_FAILED` on mismatch). Content-addressed dedup (`rd_repo.find_raw_asset_by_hash`, scoped per dataset) makes a retry idempotent regardless of Idempotency-Key. Unsupported type/empty/oversized file → `RESEARCH_DATA_FILE_TYPE_NOT_ALLOWED`/`VALIDATION_ERROR`/`RESEARCH_DATA_FILE_TOO_LARGE`; storage failure → `RESEARCH_DATA_UPLOAD_STORAGE_FAILED`. Frontend: `ResearchData.tsx`'s `UploadComposer` replaces the manual object-key/digest/size form with a native file chooser (real progress/cancel/retry), reusing the shared `frontend/src/lib/upload.ts` primitive (F-01) via `rawUploadPath`/`invalidateAfterRawUpload`; no storage metadata is ever requested from the user. The DR3 Approved-Market-Data dependency link, time-alignment/available-time policy, provenance, and revision approval all stay wired through the existing revision workflow. No migration (`research_raw_asset.object_key`/`content_digest`/`size_bytes` columns already existed). Tests: +5 backend unit (validators), +6 backend integration (happy path, dedup, unsupported type, non-owner, storage failure, digest mismatch), +2 backend contract (pure-validation-before-DB), +1 frontend component rewrite (native upload + finalize) +1 new (unsupported-type error + retry). OpenAPI snapshot regenerated for the multipart route. |
| **F-03** | **Replace simulated file inputs** | **P0** | **Complete (this PR)** | Fixed: the four text-asset upload routes (`POST /trading-signals/source-assets`, `/trade-logs/source-assets`, `/admin/manual/documents:upload`, `/create-package/requests/{id}/baseline`) now accept a real file as `multipart/form-data` (was JSON `content:str`); a new shared route-level gate `apps/api/upload.py::validate_multipart_upload` bounds the read (empty/oversize → `UPLOAD_TOO_LARGE`), enforces UTF-8 + NUL-free encoding (`UPLOAD_ENCODING_INVALID`) and, for the CSV surfaces, a non-blank header row (`UPLOAD_SCHEMA_INVALID`) BEFORE the command touches storage; the per-surface extension gate stays in the command (documented `FILE_TYPE_NOT_ALLOWED`/`MANUAL_FILE_TYPE_UNSUPPORTED` codes preserved). Frontend: the TS/TL content textareas + manual filename/content entry + CP baseline FileReader step are all replaced by a native `<input type=file>` that transfers the real bytes via the F-01 `lib/upload.ts::uploadFile` primitive (extended with an additive `headers` option so the CP baseline keeps its `X-Request-Version` OCC token). Manual/CP OCC + metadata ride multipart form fields. No migration; `ENGINE_VERSION` unchanged. Tests: +10 validator unit (`test_upload_validation.py`), +11 route contract (TS/TL/manual/CP: guest/admin-gate + bad-extension + non-UTF-8 + blank-schema + malformed-metadata, all before-DB), OpenAPI snapshot regenerated, 4 frontend component tests reworked to multipart XHR (`xhrStub` extended with a `fields` capture), +1 browser E2E spec (`07-file-uploads.spec.ts`, requires the live stack). Branch `feat/v18-f03-file-choosers`. |
| **F-04** | **Execute full Mainboard composition** | **P0** | **Complete (this PR)** | CONFIRMED gap: `jobs/backtest_engine.py::_resolve_primary_strategy` ran ONLY the first enabled Strategy — a two-strategy Mainboard silently dropped one. Fixed: `_resolve_enabled_strategies` resolves EVERY enabled Strategy in the immutable snapshot (manifest pin order — doc 01 §5.2 presentation `position` is explicitly NOT engine priority), `_prepare_and_run_strategy` runs each over its own pinned market revision / F-05 range+instrument filter / F-06 indicator plan / allocation sleeve, and a new pure `domain/backtest/engine.py::combine_item_runs` composes them into ONE portfolio result: net_profit additive, trades+decision events concatenated+re-sequenced+tagged with their originating `item_id`, portfolio equity curve built in deterministic pin order. Every participating revision stays pinned in the manifest; each object's contribution is traceable in `diagnostics.composition.items` (per-item net_profit / trade seq-range / executed flag) + the item-tagged decision trace. A selected Strategy that fails to resolve fails the WHOLE run (never silently dropped — F-04/F-06). Enabled Trading Signal / Trade Log items are pinned + recorded as participating-but-non-executing (their V1 execution effect is defined only as a Strategy data input — honest boundary, surfaced not faked). Disabled items excluded (doc 01 §5.2). A lone enabled Strategy stays byte-identical (worker skips the compose step). `ENGINE_VERSION → backtest-engine-v3-full-composition` (execution_key namespace shift, INF-04/05). No migration (per-item traceability rides `diagnostics` JSON). F-24: +2 integration (`test_backtest_persistence.py`: two-strategy runs both + composes one result with 20000 pooled independent capital; disabled-item exclusion collapses to a 10000 single-strategy result) + 3 unit (`test_backtest_engine.py`: sum/union + item-tagged trace, order-invariant net_profit, non-executing TS recorded without contribution). Honest boundary: the composite portfolio equity curve concatenates each strategy's realized-PnL progression in deterministic pin order — a genuine unified-clock multi-item co-simulation across heterogeneous bar sources stays deferred (surfaced as the L4 `portfolio_curve_sequential_not_unified_clock` diagnostic, never hidden). |
| **F-05** | **Apply date range + instrument to engine** | **P0** | **Complete (this PR)** | Fixed: worker (`jobs/backtest_engine.py`) parses+validates `backtest_range` (invalid/unparseable ISO → `RUN_FAILED_INVALID_BACKTEST_RANGE`), physically filters the bar stream via `application/queries/market_bars.py::filter_bars_by_range` (inclusive `[start,end]`, UTC-normalized, drops unparseable-timestamp rows, never yields empty batches) BEFORE the engine ever sees a bar, and cross-checks the pinned market revision's dataset-level `instrument_id` (GAP-16) against the strategy's selected instrument (a real mismatch → `RUN_FAILED_INSTRUMENT_MISMATCH`; an unset/legacy revision instrument_id is honestly passed through — no per-row instrument column exists in the current single-instrument-per-revision ingestion schema, confirmed empirically via `schema_mapping.py`/`_normalize`). A filtered stream with zero bars → explicit `RUN_FAILED_EMPTY_FILTERED_RANGE` (fail-closed, never a "succeeded" empty result). `domain/backtest/engine.py::run_engine` now surfaces the ACTUAL first/last processed bar timestamps as `summary["period_start"/"period_end"]`; `bt_repo.create_result` persists them (was hardcoded `None`) — manifest range now provably matches the data actually processed (spec acceptance). `ENGINE_VERSION → backtest-engine-v2-range-instrument-filter` (execution_key namespace shift, INF-04/05 — a stale unfiltered result is never reused). 3 new `RunFailureCode` members (additive). +8 unit (`test_market_bars_range_filter.py`, DB-free pure filter/parse coverage) + 7 integration (`test_backtest_persistence.py`: period_start/end match processed bars, narrower range processes fewer bars → 0 trades [proves the filter is physical, not cosmetic], empty-filtered-range reject, unparseable-range reject, instrument-mismatch reject, matching-instrument regression) + 1 literal-version fixup in `test_backtest_engine.py`. No migration (`backtest_range`/`instrument_id` already existed in config/schema). Honest boundary: row-level multi-instrument filtering is architecturally moot today — `MarketDatasetRevision.instrument_id` is a dataset-level field (GAP-16 resolves Instrument Scope once at dataset setup) and the canonical OHLCV Parquet schema carries no per-row instrument column (`schema_mapping.py::_ESSENTIAL_SYNONYMS` has none); true multi-instrument raw ingestion is F-01/F-02/F-03 territory (a different swimlane) and would need to land first for a per-row filter to have anything to filter. |
| **F-06** | **Remove unresolved-indicator breakout fallback** | **P0** | **Complete (this PR)** | Fixed: worker (`jobs/backtest_engine.py`) resolves the indicator plan while PROVISIONING and fails closed with `RUN_FAILED_UNRESOLVED_DEPENDENCY` when `not plan.has_entry or plan.unresolved` — the breakout proxy is now structurally unreachable on the production path. Upfront RUN gate: Ready Check emits `STRATEGY_INDICATOR_UNRESOLVED` blocker (`readiness_check._resolve_strategy_indicator_issues`), so admission (`request_backtest_run` → nested Ready Check) refuses to queue. `run_engine`'s proxy retained only as a domain unit-test primitive (24 fixtures intact). No migration, no ENGINE_VERSION bump (determinism unchanged; proxy just unreachable). Tests: +2 integration (admission-blocked + worker defence-in-depth) + 1 validator unit; proxy-reliant fixtures (`test_backtest_persistence`, `test_e2e_agent_loop`, `test_readiness_persistence`, `test_gateway_parity`) reseeded with a resolvable `ta.sma` package. |
| **F-07** | **Execute every saved Strategy setting** | **P0** | **In Progress (sub-slice (a) — this PR)** | Split into serial sub-slices (a) execution timing / (b) limit orders+validity+unfilled / (c) partial fills+close_percentage / (d) scaling / (e) restrictions+filters / (f) leverage+trailing lock_in / (g) signal-strength — each its own PR, fail-closed, ENGINE_VERSION bump. **(a) LANDED (this PR):** CONFIRMED gap — the engine IGNORED `data.execution.entry_timing`/`exit_timing` entirely (`run_engine` never read `config.data.execution`) and always filled entry AND exit at the CURRENT bar's close. Fixed: a pending-fill state machine (`_Pending`) honors the timing — `current_candle_close`/`market_fill_simulation` fill immediately at the signal bar's close (byte-identical to the prior path), `next_candle_open` defers the fill to the NEXT bar's open, `next_candle_close` to the next bar's close; the "entries always at candle close / exits always full closes" assumption is removed. Fill order is deterministic: a next-open fill resolves before the intrabar stop path; a next-close fill resolves at end-of-bar so an intrabar stop pre-empts a scheduled close exit; a deferred exit cannot be pre-empted by a fresh signal, only by a stop. Unsupported timings (`intrabar_touch`, `limit_fill_simulation`, `stop_limit_priority_simulation`) FAIL CLOSED — a NEW Ready Check blocker `STRATEGY_EXECUTION_TIMING_UNSUPPORTED` (shared `execution_timing_is_modelled` predicate, mirrors the F-09 sizing pattern) refuses admission, and the engine opens no position as a backstop (L4 `execution_timing_unsupported:<entry>/<exit>` diagnostic) rather than silently imitating an intrabar/limit path over plain OHLCV (doc 02 Entry/Exit Execution: "cannot silently imitate unavailable detail" — deferred to slices (b) + a tick-data slice). `ENGINE_VERSION → backtest-engine-v4-execution-timing` (execution_key namespace shift, INF-04/05). No migration (`execution.entry_timing`/`exit_timing` already existed in config). Diagnostics add `entry_timing`/`exit_timing`/`execution_timing_modelled`/`deferred_entry_fills`/`deferred_exit_fills`. +12 tests (10 engine: current/next-open/next-close/market-fill entry + exit positive+negative, unsupported-fails-closed entry AND exit, intrabar-stop-pre-empts-deferred-close-exit; 2 readiness: unsupported blocks / supported does not) + 1 literal-version fixup. Immediate mode byte-identical → the 24 proxy fixtures + e2e/gateway (next_candle_open) stay green. **Remaining (b)-(g) still Not Started.** |
| F-08 | Logic-Based Stop end to end | P0 | Not Started | `logic_blocks` / stop-combination schema absent. |
| **F-09** | **Fail closed for unsupported sizing** | **P0** | **In Progress (this PR)** | CONFIRMED gap: unsupported sizing fell back to all-in notional (engine.py). Fixed: engine `_open` fail-closed + Ready Check `STRATEGY_SIZING_UNSUPPORTED` blocker + tests rewritten (F-24 for sizing). |
| **F-10** | **Complete decision trace** | **P1** | **Complete (this PR)** | CONFIRMED gap: the decision trace emitted only a per-trade `entry_signal` (at entry time, `detail={trade_seq}`) + a SINGLE post-run aggregate `filtered_no_entry` (`detail={reason,count}`) — a reviewer could not reconstruct why any individual position opened / did not open / closed, nor bind a decision to a bar/rule/revision. Fixed in the pure engine (`domain/backtest/engine.py`), fully ADDITIVE at the persistence/read/export layers (the `signal_event.detail` JSONB + `event_type` are generic pass-throughs — `repositories/backtest.py` persists every event, `result_artifacts.py::project_row` surfaces the full `detail` to the UI drill-down, and `export.py`/`repositories/export.py` build export bytes from the SAME `project_row`, so UI diagnostics + export carry identical information by construction, no migration). New event taxonomy (all `event_type` ≤32 chars, `DECISION_TRACE_EVENT_TYPES`): `entry_signal` (the DECISION — carries the evaluated `rule_id` per pinned block + each nested condition's pass/fail via new read-only `BlockEvaluator.condition_snapshot`, or the breakout-proxy look-back), `entry_fill` (the EXECUTION — position_seq, fill_price, size, notional, timing, deferred), `entry_scheduled`/`exit_scheduled` (deferred F-07a timing, target_bar_seq), `entry_blocked` (a wanted entry that produced no fill — `sizing_unsupported`/`sleeve_zero_capacity`), `filtered_no_entry` (now PER-OCCURRENCE with the filtered direction + `direction_restriction` reason, replacing the aggregate), `position_close` (trade_seq linkage + exit_reason + realized pnl + entry_bar_seq + holding_bars), plus the existing `stop_resolution` (F-08) and `stop_exit_collision` — the latter now ALWAYS traced (was only under `record_both_reasons`) so the same-bar tie-break decision is never lost. Every event binds to the exact bar via `bar_seq` (1-based replayed index) + `event_time`; every position's events share a monotonic `position_seq` (incremented only on a real fill — a gap is a no-fill, never a phantom position), so a reviewer reconstructs the entry_signal→entry_fill→position_close lifecycle end to end. `combine_item_runs` binds every combined event to its executing item's pinned `root_id`/`revision_id` (F-10 object-revision binding — a rule_id resolves back to the immutable Strategy/Package revision), and the whole trace is a child of the result's `ResultManifestSnapshot` (manifest binding). Diagnostics add `decision_trace_schema="v1"`, `decision_trace_event_types`, `suppressed_entries`, and an honest `unmodelled_decision_classes=[same_direction_scaling,partial_fill,partial_close]` — the V1 bar-replay engine holds at most one full-size position, so scaling/partials never occur and are surfaced, never fabricated as phantom events. `ENGINE_VERSION → backtest-engine-v6-decision-trace` (execution_key namespace shift, INF-04/05 — a stale trace-poor result is never reused; the immutable trace artifact's deterministic content changed). No migration. Tests: +8 unit (`test_backtest_decision_trace.py` — lifecycle reconstruction with linked position_seq/trade_seq/bar_seq, byte-identical trace across batch sizes [same-manifest determinism], plan-mode rule_id + condition evidence, per-occurrence filtered_no_entry, deferred schedule→fill, entry_blocked on unsupported sizing, diagnostics schema + unmodelled classes, combined-event object-revision binding) + 2 re-aligned in `test_backtest_engine.py` (ENGINE_VERSION literal; `first_trigger_wins` now traces the collision) + the `_Position` fixture in `test_backtest_logic_stop.py` (2 new lifecycle fields). Full backend suite + ruff/format/mypy green. Branch `feat/v18-f10-decision-trace`. |
| **F-11** | **Use Research Data + Funding in engine** | **P0** | **Complete (this PR)** | CONFIRMED gap: a pinned `funding_rate` Research revision was stored ONLY as a provenance edge (`strategy_revision_references`, role `funding_source`) — `domain/backtest/engine.py` + `jobs/backtest_engine.py` had ZERO research/funding references; the engine never read it. Fixed end-to-end: NEW pure `domain/backtest/funding.py` (`FundingSchedule`/`FundingRecord` + `build_funding_schedule` — resolves each native row's `available_at` from its event time via `research_data.time_policy.resolve_available_at`, so the engine only ever sees available-time-safe records; fail-closed `resolve_funding_columns` + all-dropped/`provider`/`custom`-policy → `FundingSourceInvalid`). `run_engine(..., funding=None)` applies it as a real position cost via a backward/as-of join in the replay loop: every record now AVAILABLE at a bar (`available_at <= bar_time`) fires once and, while a position is held, charges `notional * rate` (a long PAYS a positive rate, a short RECEIVES), reducing equity mid-run so funding-on vs funding-off give a verifiably different `final_equity`/`net_profit`. A record dated after the last replayed bar can NEVER fire (future-leak impossible by construction — F-24); an unparseable bar timestamp drains nothing. `funding=None` is byte-identical to pre-F-11. Worker `_prepare_and_run_strategy` resolves the schedule via NEW `application/queries/funding.py::resolve_funding_schedule` (Approved + Research-Backtest scope + funding category + content-hash gate, injectable `load_rows` → S3-free integration tests; the `funding_rate` native asset read via NEW `research_repo.get_native_asset`) and passes it into `run_engine`; an unusable source → terminal `RunFailureCode.FUNDING_SOURCE_INVALID` (never a silent zero-cost run). The used revision is pinned in the manifest (it lives inside the immutable pinned strategy config → `composition_fingerprint`) and surfaced in diagnostics (`funding_source_revision_id`); `ENGINE_VERSION → backtest-engine-v7-research-funding` (execution_key namespace shift — a stale funding-blind result is never reused, INF-04/05). F-10 decision trace extended with a `funding_charge` event (bar_seq + rate + charge + available_at + source_revision_id). No migration. Tests: +8 unit (`test_backtest_funding.py`: on/off differ, future-leak no-fire, fixed-delay offset gates firing, charge-only-while-held, long-pays/short-receives, batch determinism, empty≡off, trace events) + 11 unit (`test_funding_schedule.py`: same-event/fixed-delay resolution, column fail-closed, drop-vs-fail-closed, provider/custom fail-closed, `parse_utc`) + 7 integration (`test_funding_resolution.py`: Approved happy path + every provenance fail-closed branch, S3-free via injected loader). Full backend suite + ruff/format/mypy green. Branch `feat/v18-f11-research-funding-engine`. **Honest boundary:** this lands the documented funding cost rule + the reusable anti-lookahead as-of join every future Research consumer reuses; a general "arbitrary Research feature → strategy condition" binding stays gated on the feature-definition compiler (raw binding prohibited, doc 12 §9.2) — a separate slice, not this one. |
| **F-12** | **Create Package frontend lifecycle** | **P0** | **Complete (this PR)** | CONFIRMED: every backend transition of `routes/create_package.py` was ALREADY reachable from `pages/CreatePackage.tsx` (Pre-Check, C.D.P = generate-candidate→draft, Upload baseline CSV, Parse baseline, Run Validation Tests, Request Revision, Approve & publish, plus the immutable validation-run / dependency-scan / baseline-asset viewers via `lib/createPackage.ts` hooks). The real gap the v18 audit named — the acceptance criteria — was that the buttons were NOT gated by the lifecycle state machine: Approve was enabled on any request with a draft revision (`disabled={!hasDraft}`), Request Revision was always enabled (`disabled={anyPending}`), Run Validation on mere draft presence — so a draft could *attempt* approval directly (the server rejected it with `VALIDATION_REQUIRED`/`APPROVAL_REQUIRES_ADMIN`, but the UI offered a doomed action, a `VALIDATION_REQUIRED` dead end). Fixed: a single pure, unit-covered source of truth `lib/createPackage.ts::packageActionAvailability(detail)` mirrors the backend request state machine (`domain/create_package/state_machine.py` `_REQUEST_ALLOWED` + the per-command gates) — Pre-Check only in the six pre-candidate states, C.D.P only before a draft exists once `can_generate_candidate`, Run Validation ONLY in `draft_created` (its one inbound `VALIDATION_RUNNING` edge), Request Revision ONLY from `revision_required`/`rejected`, and Approve ONLY in `eligible_for_approval` with fresh passing evidence (`validation_fresh`) AND a parsed baseline when equivalence is claimed (`approvalBlockReason` mirrors `approve_and_publish`'s `ValidationRequired`/`ValidationStale`/`BaselineRequired` gates); baseline upload/parse frozen once `approved`. `CreatePackage.tsx` gates every button on this helper (Approve can no longer be clicked before eligibility — the F-12 core criterion) and renders a `nextStepHint` + an explicit "a draft cannot be approved directly / upload-and-parse a baseline / re-run validation" explanation so a disabled Approve is never a mystery (no dead end). UI visibility is never authorization — the Admin approve gate stays server-side (`APPROVAL_REQUIRES_ADMIN` verbatim). Frontend-only; no backend/route/OCC/query-key/hook change, no migration, `ENGINE_VERSION` unchanged. Tests: +10 pure unit (`createPackageAvailability.test.ts` — Pre-Check/C.D.P/Run-Validation/Request-Revision state gating, the draft-cannot-approve-directly property, stale-evidence + missing-baseline approval locks, terminal-freeze, next-step hint) + 1 component (`createPackage.test.tsx` — Approve disabled + Run Validation enabled + explanation shown in `draft_created`); the existing Admin-denial component test re-aligned to the new gate (drives Approve from a real `eligible_for_approval` fixture). Frontend suite 45 files / 371 tests green, typecheck + lint clean, build green. Branch `feat/v18-f12-cp-lifecycle-gating`. |
| **F-13** | **Execute every required package validation** | **P0** | **Complete (this PR)** | CONFIRMED gap: `start_package_validation_run` built a report where `passed = all(status != failed)`, so the five stubbed `not_executed` execution checks silently passed and a bare draft reached `eligible_for_approval` — a false-pass gate. Fixed with an honest gate + real checks. **Gate (`domain/create_package/validation.py`, `VALIDATOR_VERSION → cp-validation-v2`):** `passed` iff EVERY mandatory check is `passed` (or genuinely `not_applicable`); `not_executed` AND `blocked` are non-passes and prevent approval (F-13 acceptance: "any failed, blocked, or not-executed mandatory check prevents a passed result"). A check that truly does not apply (dep-less indicator → no dependency to health-check; non-claiming package → no baseline) is `not_applicable` and excluded from the mandatory set — it never blocks. `ValidationCheck` gains an `artifacts` dict so each check carries real per-check output (resolved keys / baseline digest / syntax diagnostic), never a bare label. **Durable worker body (`application/jobs/package_validation.py`):** gathers the draft's REAL facts and runs the seven checks — `output_structure` + `dependency_health` (live ESP re-resolution, drift → fail), `syntax` (a real probe: Python `compile()`, a deterministic delimiter-balance lint for the other supplied languages; a description request has no source → `not_applicable`), `runtime` + `repaint_future_leak` (the candidate's behaviour is defined by its resolved native ta.*/cond.* plan, so a computable plan is a genuine runtime verdict and the single-pass incremental evaluators give a real non-repaint proof — an empty skeleton with no resolvable plan is `blocked` and cannot be approved, matching F-14 "empty skeletons cannot enter approval"), `real_market_data` + `baseline_comparison` (an equivalence-claiming request must carry a PASSED baseline of real market data — else both are `blocked`; a non-claiming one → `not_applicable`). The command `start_package_validation_run` delegates check execution to the worker body (keeping its OCC / immutable run row / state transition / audit / durable job row unchanged). **Honest boundary:** V1 executes the worker body in-transaction (the established CP pattern that Pre-Check / candidate-generation / baseline-parse all use) with the durable `jobs` row + immutable run row recording queued→running→passed/failed; it is shaped so a dramatiq actor can invoke it unchanged when the async plane lands (no live queue consumer added — that would rework the async lifecycle the F-12 frontend depends on). Real code execution of a *generated* candidate (F-14 sandbox) is out of scope; the resolved-plan verdict is the honest runtime/repaint check available now. No migration; `ENGINE_VERSION` unchanged. Tests: +12 unit worker probes (`test_package_validation_worker.py` — Python syntax valid/invalid, description n/a, empty-source fail, structural lint balanced/unbalanced, pinescript lint, plan probe computable/unresolvable/incompatible/condition/embedded) + `test_validation_report.py` rewritten to the strict gate (well-formed candidate passes all 7, blocked/failed/not-applicable semantics, equivalence-baseline gate) + `test_validation_evidence.py` / `test_baseline_evidence.py` re-aligned (syntax/runtime now real passes; a claim without a baseline now FAILS validation → `ValidationRequired`, not the later approve-time `BaselineRequired`). Full backend suite + ruff/format/mypy green; e2e pipeline still reaches publish (balanced pinescript + ta.rsi + no equivalence → all checks pass). Branch `feat/v18-f13-package-validation`. |
| **F-14** | **Real candidate/package generation** | **P1** | **Complete (this PR)** | CONFIRMED gap: `submit_candidate_generation` produced only a `candidate_hash` + output contract — a *hash without an implementation*; the draft `package_revision` stored `input/output_contract` + `dependency_snapshot` but no loadable body (no such column). Fixed end-to-end: NEW pure `domain/create_package/generator.py::generate_candidate` composes a **real, deterministic, loadable Python implementation** (source + entry symbol `build_signal_plan` + a compile-able test draft + the resolver-loadable plan + provenance) from the request's pinned ESP primitives + validated output contract; deterministic + order-independent, `GENERATOR_VERSION → cp-candidate-gen-v2` shifts the hash namespace (mirrors ENGINE_VERSION). An empty skeleton (no resolved primitive) is marked non-executable. Migration `0034_package_implementation` adds additive nullable JSONB `package_request.candidate_implementation` + `package_revision.implementation`; the command's Send + Request-Revision paths (shared `_generate_and_store_candidate`) pin the implementation and C.D.P copies it verbatim onto the immutable draft revision. The F-13 validation worker (`jobs/package_validation.py::_execute_generated`) now **loads + executes the generated module in a restricted sandbox** (empty `__builtins__`, no imports/I/O — only the deterministic generated module runs, never the submitted arbitrary source) and feeds an `EXEC_*` verdict into the runtime check (`VALIDATOR_VERSION → cp-validation-v3`): a hash without a loadable implementation, or an empty / non-executable skeleton, is BLOCKED/FAILED and can never reach approval (F-14 acceptance). Honest boundary: a real LLM/arbitrary-code generator + its OS-level isolated sandbox stays Future-Dev; V1 executes only our own deterministic native-plan module (safe by construction). No `ENGINE_VERSION` change. Tests: +12 unit (`test_candidate_generator.py` — generated source compiles + execs in the sandbox to the plan, determinism/order-independence, namespace shift, provenance, empty-skeleton non-executable, contract fail-closed, sandbox absent/empty/error/isolation) + 3 unit report cases (`test_validation_report.py` — hash-without-implementation blocks, empty-skeleton blocks, non-executable fails) + 1 integration (`test_create_package_persistence.py` — draft revision carries a loadable+executed implementation; validation PASSED only because the sandbox executed it). Migration up/down/up + model↔migration parity verified. ruff + format + mypy clean. Branch `feat/v18-f14-real-candidate-generation`. |
| F-15 | Replace Mainboard JSON editor | P0 | Complete | Removed the raw-JSON "Advanced: create work object" card (object-kind select + JSON payload) and the manual `wor_…` "Pin revision" input from `pages/Mainboard.tsx`. Add menu = typed product actions only (Add Strategy / Add Package / Add Outsource Signal ▸ TS·TL / Portfolio), matching the v18 prototype. Revision selection is user-facing via the inline Strategy Details editor (revision history + auto re-pin on save); no manual id is entered. Tests re-aligned + F-15 regression guards; 375 vitest green, tsc/lint/build clean. Honest boundary: pinning a newer external TS/TL revision needs a future work-object revisions-list endpoint (no such API exists yet), so that affordance was removed rather than left as a manual-id leak. |
| F-16 | Bind RUN to real readiness | P0 | Complete | RUN is now genuinely locked until a current Ready Check passes, gated on the SAME server readiness projection the backend admission authz already enforces (`request_backtest_run` runs a fresh nested Ready Check and refuses to queue with 422 `READINESS_BLOCKED` when `blocker_count > 0`). New presentation predicate `isReadyForRun(state)` in `lib/mainboard.ts` (runnable ⇔ `ready` \| `ready_with_warnings`; NOT_CHECKED/NOT_READY/CHECKING/STALE/SUPERSEDED lock) — pure presentation, no data logic/route/OCC/hook change; server stays authoritative (defense-in-depth). `pages/Mainboard.tsx` Composition card: when not runnable, RUN renders a real `disabled <button>` (out of tab order, unreachable by pointer OR keyboard — the `/backtest/run` route is not navigable) with `aria-describedby` → the "available only after a current Backtest Ready Check passes" note; when runnable, the navigable `<Link to="/backtest/run">` returns. `pages/BacktestRun.tsx` RunWorkbench: a readiness `StatusBadge` (server projection) + the "Request Backtest Run" admission button `disabled` when not runnable (visual+keyboard parity with the 422 authz instead of a click round-trip) + a locked note linking to Ready Check. Visual matches the v18 mockup `.run-button.locked` (dimmed + not-allowed via the existing `.btn:disabled`). +4 vitest (Mainboard locked-button/unlocked-link/ready_with_warnings; BacktestRun admission-locked-no-POST) → 380 green; tsc/lint/build clean. Honest boundary: in-browser screenshot proof unavailable in this environment (no backend behind the Vite dev server — all `/api/v1/*` return the SPA HTML fallback), so behavior is proven by the vitest suite rendering the real components against a stubbed API; the fixed bottom-right Ready Check/RUN shell relocation stays UI-14/UI-15 (this slice binds readiness, not the shell layout). |
| F-17 | Headline metrics on Mainboard | P1 | Complete | The Mainboard's inline latest-result summary now consumes the backend `headline` projection (`DefaultMainboard.latest_result_summary.summary.headline`, the raw summary dict the `/mainboards/default` query already carries — no route/key/OCC/hook/API change). New presentation-only helpers in `lib/mainboard.ts`: `HEADLINE_METRICS` (the five required metrics — Net Profit / Max Drawdown / ROMAD / Win Rate / Profit Factor — in the v18 mockup display order, each mapped to its raw `headline` key + the `value_format` the server metric registry declares in `domain/backtest/metrics.py::DEFAULT_METRICS`: net_profit→`net_profit_pct`/signed_percent, max_drawdown→`max_drawdown_pct`/signed_percent, romad→`romad`/decimal2, win_rate→`win_rate`/percent, profit_factor→`profit_factor`/decimal2) + `formatHeadlineMetric` (reuses the existing `formatMetricValue` so the inline values format IDENTICALLY to the Result detail + History digests — F-17 "values match the result detail"; a missing/non-finite value → explicit `HEADLINE_NA` = "N/A", never a fabricated 0, L4). `pages/Mainboard.tsx` `LatestResultCard` renders the mockup `.metric-grid`/`.metric-box`/`.metric-label`/`.metric-value` tiles (classes already in `global.css` — no CSS change) inside a `role="group" aria-label="Headline metrics"` region, ABOVE which the existing symbol · timeframe · trade count · period line is preserved verbatim (F-17 "preserve symbol, timeframe, trade count, and date"). +2 vitest (populated headline → 5 formatted metrics asserted via `within(group)`; partial headline → present values format + 3 omitted metrics render explicit N/A) → 381 green; tsc/lint/build clean. Honest boundary: in-browser screenshot proof is unavailable in this environment (same as F-16 — no backend behind the Vite dev server, so `/mainboards/default` fails and the page renders its graceful ErrorState with the metric-grid path unreachable; confirmed no console crash). Behavior is proven by the vitest suite rendering the real components against a stubbed API. |
| **F-18** | **Durable/discoverable Strategy drafts** | **P1** | **Complete (this PR)** | CONFIRMED gap: a draft's only handle was the create-time `?draft=` URL — no owner-scoped list existed, so a closed browser / re-login lost the draft (UI-02's honest boundary). Fixed with the "ufak eki" backend list + a discovery UI. **Backend (additive, no route/OCC/key change):** `queries/strategy.py::list_strategy_drafts` (owner-scoped, `require_authenticated` → Guest 401; Admin sees every owner; drafts never leak across users) over new repo `list_strategy_drafts_for_owner` (draft ⋈ strategy_root ⋈ entity_registry, soft-deleted roots excluded, newest-edit-first, a single-statement correlated `EXISTS` computes per-draft `is_attached` — no N+1) + new `GET /strategy-drafts` (collection; distinct path from `/strategy-drafts/{id}`). Each row carries `display_name`/`has_revision`/`is_attached`/`updated_at` so the UI gates attach/delete without a round-trip. **Frontend (presentation over reused endpoints):** `lib/strategy.ts` `StrategyDraftSummary` + `useMyStrategyDrafts` (key `["strategy","drafts","mine"]`, swept by the existing `["strategy"]` invalidations); `pages/StrategyDetails.tsx` NEW "My drafts" card on the no-identifier view — **open** any draft in place (removes the retained-URL dependency), **attach** a saved-unattached draft via a contextual link to the Mainboard (attach is a Mainboard composition op, CR-01 — no new attach endpoint), **delete** an UNATTACHED draft via a two-step confirm reusing the bound `useSoftDeleteWorkObject` (soft-delete → Trash, restorable → ownership/audit/history preserved); attached drafts show a badge + View link, never Delete. **No new CSS** (reuses `.card`/`.metrics-table`/`StatusBadge`/`.btn`/`EmptyState`); the stale "keep the editor URL — the draft id is the only handle" note is corrected. Tests: +6 backend integration (`test_strategy_integration.py` — owner newest-first + field shape, cross-user no-leak, Admin-sees-all, saved+attached flags, soft-deleted excluded, Guest rejected) → 23 strategy integration green; +3 frontend vitest (`strategy.test.tsx` — list+open without a kept URL, delete two-step DELETE `/work-objects` + Idempotency-Key, attach-link for saved-unattached / no Delete for attached) → 384 frontend vitest. Backend ruff+format+mypy (343 files) + tsc + lint + build clean. Branch `feat/v18-f18-durable-strategy-drafts`. **Honest boundary:** one-click attach FROM the list is deliberately a Mainboard link, not a direct attach — the draft summary exposes no mirror work-object revision id (attach needs it) and inventing that read model is out of the "ufak eki" scope; attach stays where composition lives (doc 01). |
| **F-19** | **Remove infra IDs/JSON from Strategy Details** | **P1** | **Complete (this PR)** | CONFIRMED gap: the Strategy Details editor still asked the user to TYPE opaque infra identifiers — `DataExecutionCard` (`components/StrategyConfigForm.tsx`) had six raw text inputs (Market dataset root id / revision id / content hash + Funding source root id / revision id / content hash) and `PackagePicker`'s pinned view printed the three package ULIDs (root/revision/hash) as its primary surface. Fixed **presentation-only** (no route path, react-query key, OCC token, Idempotency-Key, hook, SSE taxonomy, API call, or `lib/*.ts` data-logic change; `app/nav.ts` untouched): **(A) NEW `components/DatasetPicker.tsx`** — a browse-by-name picker over the EXISTING Market Data registry (`useMarketDatasets`, read-only, reuses the `.pkg-picker-*` CSS) that lists datasets by title/instrument and pins the SAME three flat config fields the raw inputs wrote (`market_dataset_root_id/revision_id/content_hash`, `funding_source_*`) via the untouched `mergeFlatSections` — the six raw ID inputs are removed from the primary surface; the exact identifiers move to a collapsed "Technical identifiers" `<details>` disclosure (verifiable, no longer primary); a row with no content hash pins an empty hash (never invented). **(B) `PackagePicker`** pinned view resolves the pinned package's human NAME (via the existing `useLibraryPackage` detail read) as the primary label and demotes the root/revision/hash ULIDs into the same collapsed "Technical identifiers" disclosure — the ref VALUE is unchanged. Reference chains + parameter overrides stay in the collapsed Advanced (raw JSON) editor — surfacing them as structured forms would require forbidden `lib/*.ts` (`strategyGraph.ts`) extract/merge data-logic edits (the CLAUDE.md UI rule), and the spec explicitly permits "raw JSON sadece advanced view"; restrictions already have a structured `RestrictionsCard` form. The server re-validates every pinned revision on Validate / Save (the pickers are a convenience, never authorization). Tests: +6 vitest (`datasetPicker.test.tsx` — empty state has no raw-ID textbox, browse-by-name pins root/revision/hash on pick, search filters the list, absent content-hash pins empty, pinned dataset resolves to its name with ids in the disclosure, Clear resets the ref) + 2 vitest (`packagePicker.test.tsx` — pinned summary shows the resolved package name with the ULIDs demoted to the disclosure, generic fallback before the name resolves); existing `strategyForm.test.tsx` / `strategyGraph.test.tsx` stay green (32) — the empty-payload cards never fire the registry query and the picked id now lives in the disclosure. Frontend suite 47 files / 392 vitest green, tsc + lint + build clean. Branch `feat/v18-f19-strategy-picker-form`. **Honest boundary:** the DatasetPicker/PackagePicker live inside the live draft editor (a created draft → `DraftEditorGrid`), which needs a running backend; this environment has no backend behind the Vite dev server (Package Library returns the SPA HTML fallback → "Unable to load"), so full in-browser screenshot proof of the picker is unavailable — behavior is proven by the vitest suite rendering the REAL components against a stubbed API, and the Strategy Details shell was confirmed to render with no console errors. The registry list is the first keyset page — an off-page pinned dataset resolves to a generic label with its identifiers still in the disclosure. |
| F-20 | Real autonomous Alpha Agent executor | P1 | Not Started | No durable QUEUED-task executor loop. |
| F-21 | Real Trash re-authentication | P0 | Not Started | CONFIRMED: any non-empty string accepted (deletion.py:530). Needs real IdP verify (infra-gated). |
| **F-22** | **Production authentication profile** | **P0** | **In Progress (this PR) — blocked-needs-stack for full E2E** | CONFIRMED gap: `AUTH_MODE` defaulted `dev` with no guard against `ENTROPIA_ENV=staging\|production` also running `dev` (client-controlled `X-Actor-Id` trusted outright in that combination). Fixed at the config layer: `Settings._restrict_dev_auth_to_local` (pydantic `model_validator`, fail-closed) now REFUSES to construct — i.e. the app refuses to start — whenever `ENTROPIA_ENV != local` and `AUTH_MODE == dev`; `dev` is now structurally restricted to the named local profile. `AUTH_MODE=session` (real argon2id login + opaque Bearer session, fresh server-resolved role every request, bare `X-Actor-Id` ignored — PR #38 local auth, unchanged) remains the only auth mode staging/production can run. Proven empirically: 8 new unit tests (`tests/unit/test_settings.py`, all env×mode combinations) + a new production-*profile* integration test (`tests/integration/test_auth.py::test_production_profile_rejects_bare_actor_header_impersonation`, `ENTROPIA_ENV=production` + `AUTH_MODE=session`, not just the mode in isolation) proving a bare `X-Actor-Id` header resolves to an anonymous actor and a mismatched header on a valid Bearer session is ignored. **Honest boundary — NOT claimed Complete:** the full E2E acceptance (real login/logout/session-expiration/role-denial/audit against the running Docker stack + IdP) is `blocked-needs-stack` on this machine (no Docker available this session, per CLAUDE.md environment boundary) — deferred to F-23's Playwright-against-Docker suite. |
| F-23 | Real browser E2E suite | P0 | Not Started | No Playwright suite against the Docker stack. |
| F-24 | Replace tests approving incorrect behavior | P0 | In Progress | Sizing all-in tests rewritten to fail-closed with F-09 (#209). Breakout-fallback tests done with F-06: proxy-reliant fixtures reseeded with resolvable packages + fail-closed worker/admission tests added. Remaining incorrect-behavior tests tracked per future slice. |
| F-25 | Truthful README/status | P3 | Not Started | README claims "Production V1 complete"; contradicts this backlog. |
| **UI-01** | **Mainboard inline type-specific editor + prototype Add menu** | **P0** | **Complete (this PR)** | Every item is a long horizontal row + right-side expand arrow (▼/▲, presentation-only — §3.3). Expand now reveals the **type-specific editor** entry (doc 01 §3.1 inline details host → Strategy Details / external-data details), not a raw technical dump: a primary "Edit in {Strategy Details \| Trading Signal \| Trade Log} →" deep-link keyed by the work-object **root id** (never the label — §14 rule 3) into the real editor page (`/strategy?strategy=`, `/trading-signal?root=`, `/trade-log?root=`), plus a strategy-only "View pinned revision" (`/strategy?revision=`); the Mainboard-owned composition controls (pin/enable/reorder/label/soft-delete, each OCC-guarded by `expected_row_version`) move under a labeled "Composition controls" region. The header launcher is the prototype Add menu (§3.2): Add Strategy → `/strategy`, Add Package → `/packages/create`, nested **Add Outsource Signal** (TS/TL external draft), Advanced work object, Portfolio / Equity Allocation → `/portfolio`. Ready Check / RUN / Result stay in Mainboard context. **Presentation-only:** no route path, react-query key, OCC token, Idempotency-Key, hook, SSE, API call, or `lib/*.ts` change; `app/nav.ts` verbatim. Tests re-aligned to the new markup (OCC/Idempotency assertions unchanged) + 2 new (type-specific deep-link, Add-menu links) → 373 frontend vitest green; tsc + build clean. Branch `feat/v18-ui01-mainboard-inline-editor`. |
| **UI-02** | **Strategy Details 3-column inline editor** | **P0** | **Complete (this PR)** | CONFIRMED gap: the UI-01 expand panel only offered a deep-link ("Edit in Strategy Details →") into the separate `/strategy` page — the row never opened inline, and the standalone page rendered a flat vertical stack (`StrategyConfigForm`/`StrategyGraphForm`/raw-JSON `PayloadEditor`) instead of the mockup's SETUP & DATA / DECISION LOGIC / RISK MANAGEMENT 3-column, 10-numbered-section layout with a sticky Save/Cancel/Validate/revision toolbar. Fixed: `StrategyConfigForm.tsx` and `StrategyGraphForm.tsx` split their one monolithic form each into independently-appliable, numbered section cards (`DataExecutionCard`/`ProtectionStopCard`/`PositionSizingCard`/`ConflictCard` and `PositionEntryCard`/`PositionExitCard`/`LogicBasedStopCard`/`ScalingCard`/`RestrictionsCard` — same `extractFlatSections`/`mergeFlatSections`/`extractGraphSections`/`mergeGraphSections` pure functions from `lib/strategyForm.ts`/`lib/strategyGraph.ts`, UNCHANGED); NEW `components/StrategyDetailsPanel.tsx` composes them into the real `.details-grid` (3 columns, real DOM nesting per column — not CSS-Grid auto-placement guesswork) with numbered cards 1–9 + a sticky `.panel-actions` bottom toolbar ("10. Save / Package Actions": Validate / **Save Strategy Revision** [renamed from the legacy V18 "Save as Strategy Package" label per doc 02 §10] / Clear draft… / Cancel) + a collapsed "Revision history" disclosure; raw JSON is demoted to a collapsed "Advanced (raw payload)" disclosure — never the primary surface (doc 02 UI-02 "raw JSON DEĞİL"). The SAME panel now mounts **inline inside the Mainboard row** (`pages/Mainboard.tsx` `ItemRow`, replacing the deep-link-only stub for `item_kind==="strategy"`) — the primary interaction is no longer a separate `/strategy` page; a "Open full page ↗" link stays as a back-compat escape hatch. `pages/StrategyDetails.tsx` (the standalone route) now delegates its 3 identified-id branches (`?draft=`/`?revision=`/`?strategy=`) to the SAME panel component — zero duplicated logic between the inline and full-page paths. **Presentation-only:** every hook/OCC token (`expected_draft_row_version`)/Idempotency-Key/query key/route path/API call in `lib/strategy.ts` is byte-identical; only component composition, CSS, and visible labels changed. **Honest boundary (unavoidable, not this slice's to fix):** the backend has NO root→draft lookup endpoint (confirmed in `lib/strategy.ts`'s own header comment) — an already-attached Mainboard Strategy item with a pinned revision therefore opens inline in a **read-only** 3-column summary (typed field labels via the same extract functions, never raw JSON) with an explicit "Edit this strategy (new draft)" action; starting that draft is honestly a **blank** draft (same limitation the standalone page's `CreateStrategyCard` already has), never a silent clone of the pinned revision's values — closing this gap needs a new backend capability and is tracked separately (F-18 "Durable/discoverable Strategy drafts"). CSS: `.details-grid` reflows to a single column under 980px (verified live in-browser at 700/1280/1920px — a static fixture reusing the real `global.css` confirmed the 3-column split, per-column independent card stacking, and sticky-toolbar-stays-fixed-while-content-scrolls behavior; jsdom/vitest cannot compute real CSS Grid layout, so this real-browser check was required). Tests: `strategyForm.test.tsx`/`strategyGraph.test.tsx` re-targeted at the new exported cards (structural rewrite, same `extractFlatSections`/`mergeFlatSections` assertions); `strategy.test.tsx` realigned to the new panel markup (every OCC/Idempotency/request-body assertion preserved byte-for-byte, only text-matching assertions changed); `mainboard.test.tsx`'s UI-01 deep-link-only assertion replaced with an inline-editor assertion (3 new API stubs: `GET /strategy-revisions/{id}`, `GET /strategies/{root}`, `GET /strategies/{root}/revisions`) → 374 frontend vitest green; tsc + build + lint clean. Branch `feat/v18-ui02-strategy-details-inline`. |
| **UI-04** | **Trading Signal 2-column inline panel** | **P0** | **Complete (this PR)** | CONFIRMED gap: `pages/TradingSignal.tsx` rendered a flat vertical `.card` stack (Upload → Import Request → Import Report → Create → Attached), not the mockup's 2-column SOURCE / BULK IMPORT panel closed by a sticky bottom toolbar. Fixed: the workbench is now the doc 04 §3 2-column inline panel reusing the UI-02 `.details-grid`/`.column-title`/`.detail-card`/`.panel-actions` shell — LEFT column "Trading Signal Source" groups the signal identity (source asset id / instrument id / source timezone) + optional column mapping + the "Request import" action (mockup §1 IDENTITY + §2 SOURCE); RIGHT column "Bulk Trading Signal Import" holds the real F-03 TXT/CSV `<input type=file>` (upload → immutable content-addressed source asset) + a read-only monospace format guide (canonical event fields `event_id/event_time/available_time/instrument_id/direction/signal_type/source_record_id`, mockup §3) — the system never accepts a paste box; the primary "Save Trading Signal" action moves into a sticky `.panel-actions` bottom toolbar (the `?root=` detail view's "Save new revision" + "Export manifest" likewise). NEW CSS: `.details-grid.two-col` (1fr 1fr, collapses to 1 column < 980px), `.format-guide`, `.format-note`. **Presentation-only:** every hook/OCC token (`expected_head_revision_id`)/Idempotency-Key/query key/route path/`lib/tradingSignal.ts` data logic byte-identical; `app/nav.ts` verbatim. The upload→durable-202-import→report→Save & Add chain (CR-09) is unchanged. Verified live in-browser (2-column 598/678/918px at 1280/1440/1920, no horizontal overflow, single-column < 980px, sticky toolbar `position:sticky;bottom:0`). Tests: `tradingSignal.test.tsx` 7/7 green UNCHANGED (all button labels + OCC/Idempotency/request-body assertions preserved) → 374 frontend vitest green; tsc + build + lint clean. E2E `07-file-uploads.spec.ts` labels (`/Signal-event file/`, "Upload source asset", `/Source asset stored/`, `FILE_TYPE_NOT_ALLOWED`) all preserved. Branch `feat/v18-ui04-trading-signal-inline`. |
| **UI-05** | **Trade Log 2-column inline panel** | **P0** | **Complete (this PR)** | CONFIRMED gap: `pages/TradeLog.tsx` rendered a flat vertical `.card` stack (Upload → Import Request → Import Report → Create → Attached), not the mockup's 2-column SOURCE / BULK IMPORT panel closed by a sticky bottom toolbar — the near-symmetric twin of UI-04 (Trading Signal). Fixed by mirroring UI-04: the workbench is now the doc 05 §3 2-column inline panel reusing the UI-02/UI-04 `.details-grid.two-col`/`.column-title`/`.detail-card`/`.panel-actions` shell (CSS already landed with UI-04, comment `UI-04/05`) — LEFT column "Trade Log Source" groups the trade-log identity (source asset id / instrument id / source timezone) + optional column mapping + the "Request import" action (mockup §1 IDENTITY + §2 SOURCE); RIGHT column "Bulk Trade Log Import" holds the real F-03 TXT/CSV `<input type=file>` (upload → immutable content-addressed source asset) + a read-only monospace `.format-guide` (canonical trade-record columns `direction/entry_time/entry_price/exit_time/exit_price` + optional `size/fees/pnl/symbol/open/high/low/close/volume`, mockup §3) — the system never accepts a paste box; the primary "Save Trade Log" action moves into a sticky `.panel-actions` bottom toolbar (the `?root=` detail view's "Save new revision" + "Export manifest" likewise, `ExportCard` folded into the `RevisionEditor` toolbar). Twin diff vs Trading Signal: the produced object is a **record batch** (report row "Record batch" ← `record_batch_revision_id`) and a Trade Log is **historical** data — its revisions carry **no `available_time`** lookahead guard (doc 05 §10.4; the `.format-note` states this rather than the signal's mandatory-lookahead note). **Presentation-only:** every hook/OCC token (`expected_head_revision_id`)/Idempotency-Key/query key/route path/`lib/tradeLog.ts` data logic byte-identical; `app/nav.ts` verbatim. The upload→durable-202-import→report→Save & Add chain (CR-09) is unchanged. Verified live in-browser (2-column at 1280/1920, no horizontal overflow; single-column < 980px; sticky "SAVE / PACKAGE ACTIONS" toolbar with "Save Trade Log"; real "Dosya Seç" file chooser + monospace format guide). Tests: `tradeLog.test.tsx` 7/7 green UNCHANGED (all button labels + OCC/Idempotency/request-body assertions preserved) → 374 frontend vitest green; tsc + build + lint clean. E2E `07-file-uploads.spec.ts` labels (`/Trade-record file/`, "Upload source asset", `/Source asset stored|already uploaded/`) all preserved. Branch `feat/v18-ui05-trade-log-inline` (stacked on UI-04 for the shared CSS). |
| **UI-14** | **Backtest Ready Check modal + fixed shell** | **P0** | **Complete (this PR)** | The `.run-controls` Ready Check/RUN shell CSS existed but was unused — Mainboard rendered Ready Check as a `<Link to="/backtest/ready-check">` + inline RUN in the Composition card. Fixed: `pages/Mainboard.tsx` now renders the v18 prototype's fixed lower-right shell (`.run-controls`) — a "Backtest Ready Check" `.ready-button` opening an in-context **modal** (NEW `components/ReadyCheckModal.tsx`, reusing the shared focus-trapping `Modal`), a `.ready-status` strip whose color is **bound to the real readiness state** (`readyStatusTone(readyState)` from the default-Mainboard projection → green `ready` / amber `warn` / red default; no extra fetch), and the RUN `.run-button` (genuinely locked as a disabled button until a current Ready Check passes, F-16 gate preserved). The modal renders the documented **3-column Passed / Failed / Warnings** grid from the SERVER report projection only (Passed = `summary.pass_count`, no fabricated per-pass strings L4; Failed = blocker issues; Warnings = warning issues; every string verbatim) + a Run Ready Check action with the optional fingerprint OCC guard. The `/backtest/ready-check` route survives untouched as the immutable `?report=` deep-link fallback (`ReadyCheck.tsx`). **Presentation-only:** every hook (`useCurrentReadiness`/`useRunReadinessCheck`)/OCC token (body `expected_fingerprint`)/Idempotency-Key/query key/route path/`lib/readiness.ts` data logic byte-identical; `app/nav.ts` verbatim. The modal mounts only while open, so its readiness fetch is on-demand. NEW `test/readyCheckShell.test.tsx` +7 (shell strip bound to state / green when ready / 3-column server report / RUN locked / run-check Idempotency-Key + no-guard-by-default / guard sends fingerprint / Close) → 399 frontend vitest green; tsc + lint + build clean. Verified live in-browser at 1280/1440/1920 (no horizontal overflow, 3-column report, fixed bottom-right shell, amber/green/red strip, correct ✓/✗/! color coding); the `≤720px` responsive stack rule confirmed well-formed via computed `grid-template-columns` (the preview pane floors at ~980px). Branch `feat/v18-ui14-ready-check-modal`. |
| **UI-15** | **RUN & Results inline under Mainboard** | **P0** | **Complete (this PR)** | CONFIRMED gap: the `.run-controls` RUN control (unlocked branch) was a `<Link to="/backtest/run">` that NAVIGATED AWAY to the standalone workbench page — the durable run progress + the full immutable Result rendered on a *separate* page, not "Mainboard ALTINDA inline" as the v18 prototype's `addBacktestResult()` shows (the result card appends below the strategy rows). Fixed **presentation-only** (no route path, react-query key, OCC token, Idempotency-Key, hook, SSE taxonomy, API call, or `lib/*.ts` data-logic change; `app/nav.ts` verbatim): the run-progress + result rendering (`RunStatus`+`InlineResult`) is extracted VERBATIM from `pages/BacktestRun.tsx` into a shared `components/RunProgress.tsx` (byte-identical markup → `backtestRun.test.tsx` stays green; DRY — one source of truth for both surfaces). `pages/Mainboard.tsx`: the unlocked `.run-controls` RUN control becomes an inline-admission `<button>` (same visible "RUN" label; disabled while pending) that fires the SAME 202 admission hook (`useRequestBacktestRun`, `POST /mainboard-compositions/{id}/backtest-runs`, fresh Idempotency-Key) the standalone page uses — the admitted run id lives in local state, and the BACKTEST RESULTS section renders `<RunProgress>` inline (durable run status → the full immutable Result via the UNCHANGED `ResultDetail` → **Metrics + Charts + Trade List + Diagnostics + AI Review + Data Export + Manifest**, all preserved) right under the strategy rows, never on another page; readiness `warning_count` from the admission surfaces beside the panel (warnings never block, F-16). RUN lock/unlock stays bound to the real readiness state (F-16/UI-14 gate unchanged — not_ready → the same disabled `.run-button.locked`); the `/backtest/run` route survives untouched as the `?result=` immutable deep-link + standalone workbench (Results History "View" + the latest-result headline still deep-link there). Tests: `mainboard.test.tsx` two RUN-link assertions re-aligned to the new markup (RUN is now an enabled inline-admission button, not a link — OCC/Idempotency assertions unchanged) + 1 new UI-15 test (RUN admits inline with a fresh Idempotency-Key → durable progress renders → the full ResultDetail with Metrics/Charts/Trade List/Diagnostics/Data Export headings all appear inline under the Mainboard) → 400 frontend vitest green; tsc + lint + build clean. **Honest boundary:** in-browser screenshot proof of the data-loaded inline flow is unavailable in this environment (same as F-16/F-17/F-19 — no backend behind the Vite dev server, so `/mainboards/default` returns the SPA HTML fallback and the page renders its graceful ErrorState; React Query's cached error is not recoverable via the preview tooling). Behavior is proven by the vitest suite rendering the REAL Mainboard component through the full admit→progress→ResultDetail path against a stubbed API; the app was confirmed to build + load with no console errors, and the fixed lower-right RUN shell target was confirmed against the v18 mockup. Branch `feat/v18-ui15-run-results`. |
| **UI-16** | **Results History inline provenance** | **P2** | **Complete (this PR)** | CONFIRMED gap: the blue expandable result card's inline `history-details` panel showed ONLY the key-metrics table — a user could NOT verify result identity or production inputs (Strategies / Parameters / Data / date / immutable manifest) without leaving for the separate `?result=` View page (spec §UI-16 "do not hide essential provenance only behind a separate View page"). Fixed **presentation-only** (no route path, react-query key, OCC token, Idempotency-Key, hook, SSE taxonomy, API call, or `lib/*.ts` data-logic change — `lib/backtest.ts`/`app/nav.ts` UNTOUCHED, confirmed by the 3-file diff): a NEW `HistoryDetails` sub-component in `pages/ResultsHistory.tsx` enriches the inline panel. The blue horizontal card, expansion arrow, server-side sort, keyset pagination, and two-result compare are all PRESERVED (byte-identical control flow — the 8 existing history tests stay green). The panel now renders, always in the DOM (CSS collapses it, so content stays addressable): **Strategies** + **External work** (pinned `strategy_revision_refs`/`external_work_refs` as `root @ revision`), **Parameters** (the parameter-carrying pinned inputs — `package_revision_refs`, `portfolio_allocation_plan_revision_id`, `execution_context.execution_key` — with an honest note that parameters live inside each immutable strategy/package revision), **Data** (`symbol · timeframe · market_data_revision`), **Date** (completed + `backtest_range` start→end), the **Key metrics** table (unchanged), and the **Immutable manifest summary** (`manifest_hash`, `engine_version`, `composition_fingerprint`, `materialization_status`, pinned-item count). Row-level identity (metrics/Data/Date/manifest summary) comes straight from the immutable history row — NO fetch; the pinned Strategies/Parameters lazily read the immutable Result detail's `manifest_excerpt` via the EXISTING enabled-gated `useBacktestResult(isOpen ? id : null)` hook (a presentation reuse — one detail read only when a row is expanded; while it resolves the row-level provenance already stands, and an error surfaces "open View for the full manifest", never a fabricated ref). Refs are rendered VERBATIM (`refLabel` mirrors `ResultDetail`) — never re-resolved from the current Mainboard (doc 16 §15); a manifest class the V1 excerpt does not pin renders "Not separately pinned", never a fabricated ref. The arrow `aria-label` moved `Metrics for` → `Details for` (visible/semantic label only). NEW presentation CSS (`.history-provenance`/`.history-ref-list`/`.history-detail-label`/`.history-detail-note`/`.history-hash`/`.history-manifest`/`.history-detail-foot`) appended to the v18 Results History block — no existing selector changed. Tests: +1 vitest (`resultsHistory.test.tsx` — expand a row → the lazily-fetched excerpt surfaces pinned Strategies `str_root @ str_rev` + Parameters packages/execution-key/allocation, with manifest hash + fingerprint present pre-expand from the row; apiStub ORDERED, detail fragment precedes the list prefix) → **401 frontend vitest** green; tsc + lint + build clean. **Verified in-browser** (Vite dev server + a fetch stub feeding the two read-models): the blue card (`#b9ecff` / border `#4a8aa0`, matches the mockup) expands to the full provenance panel, the detail fetch fires on expand, no console errors, no horizontal overflow at 1280/1440/1920. Branch `feat/v18-ui16-results-history`. |
| **UI-17** | **Arrange Metrics: Future Version Metrics reference panel** | **P2** | **Complete (this PR)** | CONFIRMED gap: Arrange Metrics rendered ALL registry rows in one checkbox grid — the 18 future/experimental metrics appeared only as disabled checkboxes interleaved with the 9 selectable ones, so a user could not see current vs planned metric coverage as distinct surfaces (spec §UI-17: an always-visible reference-only Future Version Metrics panel, sourced from the registry's FUTURE/EXPERIMENTAL status, "gri kutu/serbest metinle hard-code edilmez"). Fixed **presentation-only** (no route path, react-query key, OCC token `expected_profile_revision_id`, Idempotency-Key, hook, SSE, API call, or `lib/*.ts` data logic change — `lib/metricProfile.ts` UNTOUCHED, confirmed by the 3-file diff; `app/nav.ts` verbatim): `pages/ArrangeMetrics.tsx` splits the ALREADY-FETCHED `useMetricDefinitions()` registry by `selectable` — SELECTABLE rows keep the existing checkbox grid (Apply / Apply & Lock / Unlock + resolved-profile behavior byte-identical), and the FUTURE/EXPERIMENTAL rows populate a NEW always-visible, non-interactive `.future-metrics-panel` (label + "Future version only" caption, no checkbox, no value — CR-07). The backend registry already seeds all 27 rows (9 selectable + 18 future, `domain/metric_profile/registry.py`), so the panel needs NO new fetch and is registry-driven, never hard-coded. NEW `.future-metrics-title/note/list/item` CSS appended to the v18 Arrange Metrics block mirroring the mockup, mapped to theme tokens (`--border`/`--text-dim`/`--bg-elev-2`/`--radius`) — no existing selector changed. Tests: `arrangeMetrics.test.tsx` one assertion re-aligned to the new markup (a future metric is now a `region "Future Version Metrics"` item via `within`, not a disabled `Show …` checkbox; OCC/Idempotency/lock assertions unchanged) → **401 frontend vitest** green; tsc + lint + build clean. **Honest boundary:** live-browser screenshot proof unavailable — the in-app browser tool was unresponsive this session (repeated 300s timeouts) and a faithful render needs the full backend+auth stack; behavior/markup are proven by the vitest DOM suite rendering the REAL component through the real hooks against a stubbed API (this repo's established method for presentation slices). Branch `feat/v18-ui17-future-metrics-panel`. |
| UI-03, UI-06..UI-13, UI-18..UI-22 | Page-by-page UI remediation | P0–P2 | Not Started | Real uploads, prototype parity across remaining pages. Frontend track. (UI-03 in flight on its own branch.) |

## Environment boundaries (this headless session)

- Backend unit + DB-backed integration tests **run** here (Postgres :5432, isolated `TEST_DATABASE_URL`).
- **Cannot fully exercise:** F-22 production auth E2E, F-23 Playwright-against-Docker, F-21 real IdP
  re-auth — these need the running Docker stack / identity provider. They are marked Not Started and
  must not be claimed Complete from unit tests alone (§ "Prohibited completion claims").

## Change log

- 2026-07-16 — UI-14 Backtest Ready Check modal + fixed lower-right shell.
  `pages/Mainboard.tsx` wires the previously-unused `.run-controls` shell (v18
  prototype bottom-right): a "Backtest Ready Check" `.ready-button` opens a NEW
  in-context modal `components/ReadyCheckModal.tsx` (reusing the focus-trapping
  `Modal`) instead of navigating; the `.ready-status` strip color is bound to the
  real readiness state (`readyStatusTone` → green/amber/red, from the loaded
  default-Mainboard projection, no extra fetch); the RUN `.run-button` stays a
  disabled control until a current Ready Check passes (F-16 gate unchanged). The
  modal renders the documented 3-column Passed / Failed / Warnings grid from the
  SERVER report only (Passed = `summary.pass_count`; Failed/Warnings = the issue
  rows split by severity, verbatim; no client-fabricated pass strings, L4) plus a
  Run Ready Check action carrying the optional `expected_fingerprint` OCC guard +
  a fresh Idempotency-Key. `/backtest/ready-check` survives as the immutable
  `?report=` deep-link fallback. Presentation-only: hooks/OCC/Idempotency/query
  keys/routes/`lib/readiness.ts`/`app/nav.ts` unchanged. NEW `test/readyCheckShell.test.tsx`
  +7 → 399 frontend vitest green; tsc/lint/build clean; verified in-browser at
  1280/1440/1920. Branch `feat/v18-ui14-ready-check-modal`.
- 2026-07-16 — UI-05 Trade Log 2-column inline panel (twin of UI-04).
  `pages/TradeLog.tsx` restructured from a flat vertical `.card` stack into the
  doc 05 §3 2-column SOURCE / BULK IMPORT panel (reusing the UI-02/UI-04
  `.details-grid.two-col`/`.column-title`/`.detail-card`/`.panel-actions` shell —
  CSS already landed with UI-04): LEFT "Trade Log Source" groups identity
  (source asset id / instrument id / timezone) + optional column mapping +
  Request import; RIGHT "Bulk Trade Log Import" holds the real F-03 TXT/CSV file
  chooser + a read-only monospace `.format-guide` (canonical trade-record columns
  `direction/entry_time/entry_price/exit_time/exit_price` + optional
  `size/fees/pnl/…`); the primary Save/Export actions move into a sticky bottom
  toolbar (`ExportCard` folded into the `RevisionEditor` toolbar). Twin diff: the
  produced object is a record batch and a Trade Log is historical — no
  `available_time` guard (doc 05 §10.4). NO new CSS (reuses UI-04's classes).
  Presentation-only: every hook / OCC token (`expected_head_revision_id`) /
  Idempotency-Key / query key / route / `lib/tradeLog.ts` byte-identical;
  `app/nav.ts` verbatim. `tradeLog.test.tsx` 7/7 green UNCHANGED → 374 frontend
  vitest; tsc + build + lint clean; E2E `07-file-uploads.spec.ts` labels
  preserved. Verified live in-browser at 1280/1920 (2-column, no overflow;
  single-column < 980px; sticky toolbar). Branch
  `feat/v18-ui05-trade-log-inline` (stacked on UI-04 for the shared CSS).
- 2026-07-16 — UI-04 Trading Signal 2-column inline panel. `pages/TradingSignal.tsx`
  restructured from a flat vertical `.card` stack into the doc 04 §3 2-column
  SOURCE / BULK IMPORT panel (reusing UI-02's `.details-grid`/`.column-title`/
  `.detail-card`/`.panel-actions` shell): LEFT "Trading Signal Source" groups
  identity (source asset id / instrument id / timezone) + optional column mapping
  + Request import; RIGHT "Bulk Trading Signal Import" holds the real F-03 TXT/CSV
  file chooser + a read-only monospace format guide (canonical event fields); the
  primary Save/Export actions move into a sticky bottom toolbar. NEW CSS
  `.details-grid.two-col` / `.format-guide` / `.format-note`. Presentation-only:
  every hook / OCC token (`expected_head_revision_id`) / Idempotency-Key / query
  key / route / `lib/tradingSignal.ts` byte-identical; `app/nav.ts` verbatim.
  `tradingSignal.test.tsx` 7/7 green UNCHANGED → 374 frontend vitest; tsc + build +
  lint clean; E2E `07-file-uploads.spec.ts` labels preserved. Verified live in
  browser at 1280/1440/1920 (2-column, no overflow; single-column < 980px; sticky
  toolbar). Branch `feat/v18-ui04-trading-signal-inline`.
- 2026-07-16 — UI-02 Strategy Details 3-column inline editor. The UI-01 expand
  panel only deep-linked to `/strategy`; the standalone page itself was a flat
  vertical stack with raw JSON as a co-equal editor. `StrategyConfigForm.tsx`/
  `StrategyGraphForm.tsx` split into 9 independently-appliable numbered section
  cards (same `extractFlatSections`/`mergeFlatSections`/`extractGraphSections`/
  `mergeGraphSections` pure functions, unchanged); NEW `StrategyDetailsPanel.tsx`
  composes them into the real 3-column `.details-grid` (SETUP & DATA / DECISION
  LOGIC / RISK MANAGEMENT, real per-column DOM nesting) + a sticky "10. Save /
  Package Actions" toolbar (Validate / **Save Strategy Revision** [renamed from
  V18's legacy "Save as Strategy Package"] / Clear draft… / Cancel); raw JSON
  demoted to a collapsed "Advanced (raw payload)" disclosure. The panel now
  mounts inline inside the Mainboard row (`ItemRow`, `item_kind==="strategy"`)
  — the primary interaction is no longer the standalone page, which now reuses
  the SAME panel for its 3 identified-id modes. Presentation-only: every hook/
  OCC token/Idempotency-Key/query key/route in `lib/strategy.ts` unchanged.
  Honest boundary: no backend root→draft lookup exists, so an already-attached
  item opens a **read-only** summary with an "Edit this strategy (new draft)"
  action that starts blank (same limitation the standalone create flow already
  had) — closing it is F-18's job. Verified live in-browser (700/1280/1920px,
  a static fixture reusing the real global.css — jsdom can't compute real CSS
  Grid layout). `strategyForm.test.tsx`/`strategyGraph.test.tsx` re-targeted at
  the new cards; `strategy.test.tsx`/`mainboard.test.tsx` realigned to the new
  markup (every OCC/Idempotency/body assertion preserved byte-for-byte) → 374
  frontend vitest green; tsc + build + lint clean. Branch
  `feat/v18-ui02-strategy-details-inline`.
- 2026-07-16 — UI-01 Mainboard inline type-specific editor + prototype Add menu. Each item stays a
  long horizontal row + right expand arrow; the expand now opens the type-specific editor entry (doc
  01 §3.1) — a root-id-keyed "Edit in {Strategy Details | Trading Signal | Trade Log} →" deep-link
  (`/strategy?strategy=`, `/trading-signal?root=`, `/trade-log?root=`) + a strategy "View pinned
  revision" (`/strategy?revision=`) — instead of a bare technical dump, with the OCC-guarded
  pin/enable/reorder/label/delete controls grouped under "Composition controls". The header launcher
  becomes the §3.2 prototype Add menu (Add Strategy / Add Package / nested Add Outsource Signal /
  Advanced work object / Portfolio). Presentation-only: no route/key/OCC/Idempotency/hook/SSE/API/lib
  change, `app/nav.ts` verbatim. `frontend/src/pages/Mainboard.tsx` + `test/mainboard.test.tsx`
  (373 vitest green, tsc + build clean) + `e2e/pages/MainboardPage.ts` re-aligned to the new "+ Add"
  menu / "Advanced: create work object" labels. Branch `feat/v18-ui01-mainboard-inline-editor`.
- 2026-07-16 — F-14 real candidate/package generation. The candidate stub emitted a
  hash + output contract only (no loadable implementation). NEW pure
  `domain/create_package/generator.py::generate_candidate` composes a real,
  deterministic, loadable Python implementation (source + `build_signal_plan` entry +
  test draft + resolver-loadable plan + provenance) from the pinned ESP primitives +
  validated output contract; `GENERATOR_VERSION → cp-candidate-gen-v2`. Migration
  `0034_package_implementation` stores it on `package_request.candidate_implementation`
  + the immutable `package_revision.implementation`. The F-13 validation worker now
  loads + EXECUTES the generated module in a restricted sandbox (empty `__builtins__`,
  only the deterministic module runs) and blocks a hash-without-implementation / empty /
  non-executable candidate from approval (`VALIDATOR_VERSION → cp-validation-v3`). No
  ENGINE_VERSION change. +16 tests. Honest boundary: a real LLM/arbitrary-code generator
  + OS-level sandbox stays Future-Dev. Branch `feat/v18-f14-real-candidate-generation`.
- 2026-07-15 — F-11 use Research Data + Funding in the backtest engine. A pinned
  `funding_rate` Research revision was provenance-only (`strategy_revision_references`); the
  engine now READS + APPLIES it. NEW pure `domain/backtest/funding.py` builds an
  available-time-safe `FundingSchedule` (each row's `available_at` resolved from event time
  via `research_data.time_policy`); `run_engine(..., funding=)` applies it as a real position
  cost with a backward/as-of join (a long pays a positive rate, a short receives) so
  funding-on vs funding-off differ verifiably, and a value dated after the last bar can never
  fire (future-leak impossible by construction, F-24). Worker resolves the source via NEW
  `queries/funding.py::resolve_funding_schedule` (Approved + Research-Backtest scope +
  funding category + content-hash gate, S3-free injectable loader) + NEW
  `research_repo.get_native_asset`; an unusable source → terminal
  `RUN_FAILED_FUNDING_SOURCE_INVALID`. `ENGINE_VERSION → backtest-engine-v7-research-funding`;
  F-10 trace += `funding_charge`. +26 tests (8 engine + 11 schedule + 7 resolution). No
  migration. Honest boundary: general Research-feature→condition binding stays gated on the
  feature-definition compiler (doc 12 §9.2) — a separate slice.
- 2026-07-15 — F-03 replace all simulated file inputs with real native file choosers. TS/TL source
  assets, User Manual documents, and Create Package baseline now transfer real bytes via
  `multipart/form-data`; a shared `apps/api/upload.py` gate validates size/UTF-8-encoding/CSV-schema
  server-side before persistence (extension checks stay per-surface). Frontend replaces every content
  textarea / FileReader step with a native `<input type=file>` reusing F-01's `lib/upload.ts`. No
  migration. Branch `feat/v18-f03-file-choosers`.
- 2026-07-15 — F-02 real Research Data raw-asset file upload. `POST /research-datasets/{id}/upload-session`
  now transfers real multipart bytes (`UploadFile`); object key/digest/size/content-type all
  server-derived + integrity-verified via a storage read-back before persisting; content-addressed dedup
  (`find_raw_asset_by_hash`) makes retry idempotent. `ResearchData.tsx`'s `UploadComposer` reuses the F-01
  `frontend/src/lib/upload.ts` primitive (native file chooser + progress/cancel/retry) — no storage
  metadata entered by the user. DR3 Approved-Market-Data link / time policy / provenance unchanged. No
  migration. OpenAPI snapshot regenerated. Branch `feat/v18-f02-research-data-upload`.
- 2026-07-15 — F-01 real Market Data raw-asset file upload. `POST /market-datasets/{id}/raw-uploads`
  now transfers real multipart bytes; object key/digest/size/content-type all server-derived +
  integrity-verified via a storage read-back before persisting; content-addressed dedup makes retry
  idempotent. New shared `frontend/src/lib/upload.ts` (XHR multipart + progress/cancel/retry) reused
  by `MarketData.tsx`'s native file-chooser UploadComposer. No migration. Branch
  `feat/v18-f01-market-data-upload`.
- 2026-07-14 — F-09 fail-closed sizing (engine + Ready Check blocker) + F-24 sizing-test rewrite.
  Branch `feat/v18-f09-sizing-fail-closed`. `ENGINE_VERSION → backtest-engine-v2-sizing-fail-closed`.
- 2026-07-15 — F-04 execute the complete Mainboard composition. Worker runs EVERY enabled Strategy (`_resolve_enabled_strategies` + `_prepare_and_run_strategy`) and composes them into one portfolio result via the pure `combine_item_runs`; per-item contribution traceable in `diagnostics.composition`; single-strategy path byte-identical. `ENGINE_VERSION → backtest-engine-v3-full-composition`. No migration. Branch `feat/v18-f04-full-composition`.
- 2026-07-15 — F-06 remove unresolved-indicator breakout fallback. Worker fails closed with
  `RUN_FAILED_UNRESOLVED_DEPENDENCY` (proxy unreachable in production); Ready Check adds the upfront
  `STRATEGY_INDICATOR_UNRESOLVED` blocker. No migration, no ENGINE_VERSION bump. Branch
  `feat/v18-f06-remove-breakout-proxy`.
- 2026-07-15 — F-07 sub-slice (a) execution timing. The engine honored `data.execution.entry_timing`/
  `exit_timing` for the first time (it previously always filled at the current candle close): a
  `_Pending` fill state machine defers `next_candle_open`/`next_candle_close` fills to the next bar
  (immediate `current_candle_close`/`market_fill_simulation` byte-identical), and the unsupported
  `intrabar_touch`/limit/stop-limit-simulation timings fail closed via a NEW Ready Check blocker
  `STRATEGY_EXECUTION_TIMING_UNSUPPORTED` (shared `execution_timing_is_modelled` predicate) + an inert
  engine backstop. `ENGINE_VERSION → backtest-engine-v4-execution-timing`. No migration. +12 tests.
  Branch `feat/v18-f07a-execution-timing`. Remaining F-07 sub-slices (b)-(g) still Not Started.

